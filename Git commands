This is my git practice on git commands 
the commands are as follows
git init
git add
git commit -m "message"
Before push we have to add  the remote location of repo
git remote -u origin
git push origin "branch name" 
